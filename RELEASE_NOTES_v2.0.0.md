# 鸣潮抢码器 v2.0.0

## 主要更新

- 增强直播流扫描稳定性，支持直播中断后的自动重连。
- 对齐 KuRo_Scanner / MHY_Scanner 的低延迟直播流参数，减少首帧等待和缓冲延迟。
- 将直播流扫描从“每 5 帧一次 + PIL 转换”优化为“默认每 3 帧一次 + OpenCV BGR 帧直解”，降低识别路径开销。
- 为 WeChat QR 检测器设置 `scaleFactor=0.4`，贴近竞品项目的二维码检测参数。
- 统一鸣潮二维码载荷校验和 ticket 去重逻辑，减少重复提交和误识别。
- 增加登录并发保护，同一时间只处理一个二维码登录请求，重复码会被直接忽略。
- 屏幕扫描窗口加入自适应扫描频率：刚开始高频扫描，长时间未命中自动降频，识别处理中进一步降频。
- 账号列表增加状态列和“刷新状态”入口，Token 状态会写回账号数据。
- 保留短测试码兼容，同时对非鸣潮二维码噪声做过滤。
- 引入多项性能优化，包括线程池、图像缓冲池、智能 ROI 预测和性能监控。
- 修复 PyInstaller 打包配置中绑定本机 `pyzbar` 路径的问题，改为按构建环境自动定位 DLL。

## 构建信息

- 版本标签：`v2.0.0`
- 构建方式：PyInstaller `mingchao_scanner.spec`
- 构建环境：Windows 10，Python 3.14.4
- 验证：`66 passed`

## 文件

- `鸣潮抢码器-v2.0.0.exe`：Windows 单文件可执行程序
- `README.md`：项目说明
- `LICENSE`：MIT 许可证
- `SHA256SUMS.txt`：文件校验值

## 注意

本工具仅供学习交流使用。使用前请确认符合游戏服务条款和库街区用户协议。
